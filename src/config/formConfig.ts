const labelWidth = '80px'
const labelPosition = 'left'
const center = true;
export const formItemConfig = {
    center,
    labelWidth,
}
export const formConfig = {
    center,
    labelPosition
}